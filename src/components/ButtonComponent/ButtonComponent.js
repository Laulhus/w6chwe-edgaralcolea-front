const { FontAwesomeIcon } = require("@fortawesome/react-fontawesome");

const ButtonComponent = (actionOnClick) => {
  return <FontAwesomeIcon icon="fa-solid fa-trash-can" />;
};
export default ButtonComponent;
